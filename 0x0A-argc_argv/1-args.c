#include "main.h"
#include <stdio.h>

/**
 * main - Code entry point
 * @argc: Returns argument count
 * @argv: Returns argument array of string
 * Return: Code returns 0
 */

int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc - 1);
	return (0);
}
